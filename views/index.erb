<!DOCTYPE html>
<html>
<head>
  <title>Coucou</title>
</head>
<body>

<h1>Bonjour</h1>
        
        <% if @user.nil? %>
          <p>Bonjour, Veuillez vous connecter </p>
          <a href="/sauth/session/new"> Connection </a>
          <p>Pas encore inscrit? </p>
          <a href="/sauth/register"> Inscription </a>
        <% else %>
          
	  <p>Bonjour <%= @user %> </p>
            <% if !@error.nil? %><%= @error %><% end %>
            <% if @user == "admin" %><p>Vous êtes le grand maître du sauth </p><a href="/sauth/admin"> Go to admin user </a><% end %>
          <p></p>
          <a href="/sauth/application/new"> Inscrire une application </a>
          <p></p>
          <a href="sessions/deco"> Deco </a>

          <% u = User.find_by_login(@user)
             uid = u.id
             apps = Application.where(:user_id => uid)

            if !apps.empty?
          %>
 
           <div class="listappown">
             <p>List de vos applications</p>
             
            <ul>
              <% apps.each do |a| %>
                <li><%=a.name%>  : <%=a.url%> - <a href="/sauth/application/delete?app=<%=a.id%>">effacer</a> </li>
              <% end %>
            </ul>
            </div>

            <%else%>
              <p>Vous n'avez créé aucune application.</p>
            <%end%>

            <% utils = Utilisation.where(:user_id => uid)
              if !utils.empty?
            %>
            <div class="listappuse">
            <p>Listes des application que vous pouvez utiliser</p>
              <ul>
                <% utils.each do |util| %>
                  <% app = Application.find_by_id(util.application_id) %>
                  <li><%=app.name%> : <%=app.url%></li>
                <% end %>
              </ul>
            </div>

           <%else%>
             <p>Vous n'utilisez aucune application</p>
           <%end%>
        <% end %>

</body>
</html>
