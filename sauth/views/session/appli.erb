<!DOCTYPE html>
<html>
<head>
  <title>Portail de Connexion</title>
</head>
<body>

<h1>Portail de Connexion</h1>

<% if @error_con.nil? %>
		<p >Saisir votre login et votre mot de passe pour vous connecter</p>

<% else %>

<%= @error_con %>

<% end %>


<form action="/<%= @appli %>/sessions" method="post">
  <div class="field">
    <label for="session_login">Login</label><br />

    <input id="login" name="login" size="30" type="text" />
  </div>
  <div class="field">
    <label for="passwd">Mot de passe</label><br />
    <input id="passwd" name="passwd" size="30" type="password" />
  </div>
  <div class="actions">
    <input name="commit" type="Se connecter" value="Connexion" />
  </div>
  <div class="field">
    <input id="back_url" name="back_url" size="50" type="hidden" value="<%= @back_url %>">
  </div>
</form>

<p></p>
<p>Pas encore inscrit? </p>
          <a href="/users/new"> Inscription </a>

</body>
</html>

